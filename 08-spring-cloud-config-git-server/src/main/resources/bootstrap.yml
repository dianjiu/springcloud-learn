spring:
  application:
    name: config-git-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: http://git.dianjiu.cc/co-dianjiu/spring-config-demo.git #配置文件所在仓库
          username: dianjiu #登录账号
          password: dianjiu63 #登录密码
          default-label: master #配置文件分支
          search-paths: demo-config  #配置文件所在根目录